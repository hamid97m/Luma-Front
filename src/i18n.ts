import { fa } from './locales/fa.js';

// Single-locale for now. To add a language later, add src/locales/<lang>.ts
// (same shape as fa) and select it here.
export const t = fa;
export type Messages = typeof fa;
